import React from 'react';

const element = React.createElement(
    'h1',
    null,
    'I am a React element. I was produced by a createElement function.'
);

console.log('A React element', element);

export default element;
